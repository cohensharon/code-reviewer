info:
  name: Correct PR URL v1 (ticket not matching)
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/reviews
  body:
    type: json
    data: |-
      {

        "prUrl": "https://github.com/openai/openai-python/pull/3496",
        "ticketText": "When processing Anthropic `signature_delta` streaming events, ensure that empty `thinking` blocks are preserved instead of being omitted. The streaming adapter should always include the required `thinking` field in the emitted chunks (using an empty/default value when necessary) so downstream consumers receive a consistent response schema and 400 errors are avoided.",
        "reviewStrategy": "v1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
