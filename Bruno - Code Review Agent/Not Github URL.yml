info:
  name: Not Github URL
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/reviews
  body:
    type: json
    data: |-
      {

        "prUrl": "https://notgithub.com/a/b/pull/1"

      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
